# 修复：发布窗口关闭时保存登录信息不完整

**修复日期**：2026-08-20

## 问题描述

发布窗口关闭时，浏览器会自动保存最新的登录信息到后台。但是**只保存了 Cookies，丢弃了 localStorage、sessionStorage、IndexedDB 数据**。

这导致某些平台（如搜狐号、腾讯号等）在下次发布时可能出现登录状态不完整的问题，因为这些平台可能依赖 localStorage 或 IndexedDB 中的数据。

## 问题根源

### 代码位置

`main.js:18032` - `uploadSessionToBackend` 函数

### 问题代码

```javascript
async function uploadSessionToBackend({ apiOrigin, saveSessionApi, backendAccountId, cookieDomains, cookiesArray, accountInfo }) {
  const uploadCookiesArray = dedupeCookiesForSessionSave(accountInfo?.platform, cookiesArray, 'backend-upload');
  const postData = JSON.stringify({
    id: backendAccountId,
    cookies: JSON.stringify({ domain: cookieDomains[0], cookies: uploadCookiesArray })
    // ⚠️ 只包含了 cookies 数组，没有 localStorage/sessionStorage/indexedDB
  });
  // ...
}
```

### 数据流分析

1. **获取完整数据** ✅
   - `collectWindowSessionSaveContext` → `getFullSessionDataFromWebContents`
   - 已正确获取 Cookies + localStorage + sessionStorage + IndexedDB
   - 存储在 `context.sessionData` 中

2. **本地缓存保存** ✅
   - `saveLatestSessionCache` 函数接收完整的 `sessionData`
   - 本地缓存包含完整数据

3. **后台上传** ❌ **问题在这里**
   - `uploadSessionToBackend` 函数虽然接收了 `context.sessionData`
   - 但构建 POST 数据时**只使用了 `cookiesArray`，丢弃了其他数据**

### 对比：授权脚本 vs 发布窗口关闭

**授权脚本**（如 `douyin-creator.js`）提交的数据格式：
```javascript
{
  id: backendAccountId,
  cookies: JSON.stringify({
    domain: 'douyin.com',
    timestamp: 1234567890,
    cookies: [{ name, value, domain, ... }],
    localStorage: { ... },      // ✅ 包含
    sessionStorage: { ... },    // ✅ 包含
    indexedDB: { ... }          // ✅ 包含
  })
}
```

**发布窗口关闭**（修复前）提交的数据格式：
```javascript
{
  id: backendAccountId,
  cookies: JSON.stringify({
    domain: 'douyin.com',
    cookies: [{ name, value, domain, ... }]
    // ❌ 没有 localStorage/sessionStorage/indexedDB
  })
}
```

## 修复内容

### 修改的函数

`main.js:18032` - `uploadSessionToBackend`

### 修复后的代码

```javascript
async function uploadSessionToBackend({ apiOrigin, saveSessionApi, backendAccountId, cookieDomains, cookiesArray, sessionData, accountInfo }) {
  const uploadCookiesArray = dedupeCookiesForSessionSave(accountInfo?.platform, cookiesArray, 'backend-upload');

  // 🔑 构建完整会话数据（包含 localStorage/sessionStorage/indexedDB）
  let cookiesPayload;
  if (sessionData && typeof sessionData === 'object' && !Array.isArray(sessionData)) {
    // 使用完整会话数据（getFullSessionDataFromWebContents 返回的格式）
    cookiesPayload = {
      domain: cookieDomains[0],
      cookieDomains: cookieDomains,
      domains: cookieDomains,
      timestamp: sessionData.timestamp || Date.now(),
      cookies: uploadCookiesArray,
      localStorage: sessionData.localStorage || {},
      sessionStorage: sessionData.sessionStorage || {},
      indexedDB: sessionData.indexedDB || {}
    };
  } else {
    // 降级为仅 cookies 的格式（向后兼容）
    cookiesPayload = {
      domain: cookieDomains[0],
      cookies: uploadCookiesArray
    };
  }

  const postData = JSON.stringify({
    id: backendAccountId,
    cookies: JSON.stringify(cookiesPayload)
  });
  // ...
}
```

### 修复要点

1. **使用 `sessionData` 参数**：包含完整的会话数据（来自 `getFullSessionDataFromWebContents`）
2. **构建完整 payload**：包含 `localStorage`、`sessionStorage`、`indexedDB`
3. **向后兼容**：如果 `sessionData` 为空，降级为仅 cookies 的格式
4. **保持一致性**：与授权脚本的数据格式保持一致

## 影响范围

### 受益的平台

所有使用 localStorage/sessionStorage/IndexedDB 存储登录状态的平台，包括但不限于：

1. **搜狐号** - localStorage 存储用户信息
2. **腾讯号** - localStorage 存储登录凭证
3. **头条号** - localStorage 存储 app_id 等
4. **视频号** - localStorage 存储会话信息
5. **小红书** - localStorage 存储用户配置
6. 其他需要完整会话数据的平台

### 不受影响的场景

- **授权流程**：授权脚本已经正确获取并提交完整数据，不受影响
- **本地缓存**：`saveLatestSessionCache` 已经保存完整数据，不受影响
- **临时 session**：授权窗口使用临时 session，关闭后自动销毁，不经过此流程

## 测试建议

### 测试场景 1：发布后再次发布

1. 授权某个平台账号（如搜狐号）
2. 发布一篇内容
3. 关闭发布窗口（触发自动保存）
4. 立即再次打开发布窗口
5. **验证**：检查 localStorage/sessionStorage 是否正确恢复

### 测试场景 2：对比后台数据

1. 授权某个平台账号
2. 发布一篇内容
3. 关闭发布窗口
4. **验证**：后台保存的 cookies 字段是否包含 localStorage/sessionStorage/indexedDB

### 预期结果

**修复前**：
```json
{
  "domain": "mp.sohu.com",
  "cookies": [...]
}
```

**修复后**：
```json
{
  "domain": "mp.sohu.com",
  "cookieDomains": ["mp.sohu.com", "sohu.com"],
  "domains": ["mp.sohu.com", "sohu.com"],
  "timestamp": 1234567890,
  "cookies": [...],
  "localStorage": {
    "mp.sohu.com": { "user_info": "..." },
    "sohu.com": { "session_id": "..." }
  },
  "sessionStorage": { ... },
  "indexedDB": { ... }
}
```

## 相关文件

- `main.js:18032` - `uploadSessionToBackend` 函数
- `main.js:17797` - `collectWindowSessionSaveContext` 函数
- `main.js:17542` - `getFullSessionDataFromWebContents` 函数
- `main.js:18179` - `persistWindowSessionToBackend` 函数（调用点）
- `injected-scripts/*-creator.js` - 各平台授权脚本（对比参考）

## 版本信息

- **修复版本**：待发布
- **影响版本**：所有历史版本
- **向后兼容**：是（降级逻辑保证了向后兼容）
